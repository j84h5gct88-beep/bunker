const header=document.querySelector('.header');addEventListener('scroll',()=>header.classList.toggle('scrolled',scrollY>30));
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
const dialog=document.getElementById('booking');const zone=document.getElementById('zone');document.querySelectorAll('[data-book]').forEach(btn=>btn.addEventListener('click',()=>{if(btn.dataset.zone)zone.value=btn.dataset.zone;dialog.showModal()}));document.querySelector('.booking-close').onclick=()=>dialog.close();dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.close()});
const date=document.querySelector('[name=date]');date.min=new Date().toISOString().split('T')[0];
document.getElementById('booking-form').addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget);const extras=f.getAll('extras').join(', ')||'Не указано';const msg=`Здравствуйте! Хочу забронировать столик в BUNKER.%0A%0AИмя: ${encodeURIComponent(f.get('name'))}%0AТелефон: ${encodeURIComponent(f.get('phone'))}%0AДата: ${f.get('date')}%0AВремя: ${f.get('time')}%0AГостей: ${f.get('guests')}%0AФормат: ${encodeURIComponent(f.get('zone'))}%0AДополнительно: ${encodeURIComponent(extras)}%0AПожелания: ${encodeURIComponent(f.get('comment')||'Нет')}`;window.open(`https://wa.me/79191130932?text=${msg}`,'_blank','noopener')});
